{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the laipe-app. Allows the frontend to call our `chat`, `cancel`, `get_console_entries`, and `clear_console` commands and listen to the streaming / console events. No filesystem / network / shell access — only IPC.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
